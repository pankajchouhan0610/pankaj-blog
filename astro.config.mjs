import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://pankaj-blog-pi.vercel.app',
    markdown: {
        shikiConfig: {
              theme: 'github-dark',
                    wrap: true,
                        },
                          },
                          });
